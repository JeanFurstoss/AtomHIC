#include <string>
#include <map>

int compareSum(const std::string& file1, const std::string& file2, const double& tolerance = 0);

int compareExact(const std::string& file1, const std::string& file2, const double& tolerance = 1e-7);
